export interface DatabaseComment {
  createdAt: string;
  updatedAt: string;
  userId: string;
  username: string;
  userPhoto: string;
  attachedPhoto: string;
  postId: string;
  commentId: string;
  comment: string;
}
